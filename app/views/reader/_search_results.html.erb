<% results.each_with_index do |result, index| %>
  <% locals = {entry: result, index: (index + @page * 10)} %> 
  <%= render :partial => (result.shared ? "share_result" : "entry_result"), :locals => locals %>
<% end %>